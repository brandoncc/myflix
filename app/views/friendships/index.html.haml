%section.people
  %header
    %h2 People I Follow
  %table.table
    %thead
      %tr
        %th(width="30%")
        %th(width="20%") Videos in Queue
        %th(width="20%") Followers
        %th(width="30%") Unfollow
    %tbody
      - @friendships.each do |friendship|
        %tr
          %td
            %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest('knwang@gmail.com'.downcase)}?s=40")
            = link_to "#{friendship.friend.name}", user_path(friendship.friend)
          %td.extra-padding 
            = "#{friendship.friend.queue_size}"
          %td.extra-padding 
            = "#{friendship.friend.follower_size}"
          %td.extra-padding
            = link_to friendship_path(friendship), method: :delete do
              %i.glyphicon.glyphicon-remove            
              
